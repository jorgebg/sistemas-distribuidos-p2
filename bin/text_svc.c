/*
 * Please do not edit this file.
 * It was generated using rpcgen.
 */

#include "text.h"
#include <stdio.h>
#include <stdlib.h>
#include <rpc/pmap_clnt.h>
#include <string.h>
#include <memory.h>
#include <sys/socket.h>
#include <netinet/in.h>

#ifndef SIG_PF
#define SIG_PF void(*)(int)
#endif

static void
servicioprog_1(struct svc_req *rqstp, register SVCXPRT *transp)
{
	union {
		parametro2 f_ping_1_arg;
		parametro4 f_swap_1_arg;
		parametro4 f_hash_1_arg;
		parametro5 f_check_1_arg;
		parametro7 f_stat_1_arg;
	} argument;
	union {
		char f_ping_1_res;
		retorno2 f_swap_1_res;
		u_int f_hash_1_res;
		char f_check_1_res;
		int f_quit_1_res;
	} result;
	bool_t retval;
	xdrproc_t _xdr_argument, _xdr_result;
	bool_t (*local)(char *, void *, struct svc_req *);

	switch (rqstp->rq_proc) {
	case NULLPROC:
		(void) svc_sendreply (transp, (xdrproc_t) xdr_void, (char *)NULL);
		return;

	case F_PING:
		_xdr_argument = (xdrproc_t) xdr_parametro2;
		_xdr_result = (xdrproc_t) xdr_char;
		local = (bool_t (*) (char *, void *,  struct svc_req *))f_ping_1_svc;
		break;

	case F_SWAP:
		_xdr_argument = (xdrproc_t) xdr_parametro4;
		_xdr_result = (xdrproc_t) xdr_retorno2;
		local = (bool_t (*) (char *, void *,  struct svc_req *))f_swap_1_svc;
		break;

	case F_HASH:
		_xdr_argument = (xdrproc_t) xdr_parametro4;
		_xdr_result = (xdrproc_t) xdr_u_int;
		local = (bool_t (*) (char *, void *,  struct svc_req *))f_hash_1_svc;
		break;

	case F_CHECK:
		_xdr_argument = (xdrproc_t) xdr_parametro5;
		_xdr_result = (xdrproc_t) xdr_char;
		local = (bool_t (*) (char *, void *,  struct svc_req *))f_check_1_svc;
		break;

	case F_STAT:
		_xdr_argument = (xdrproc_t) xdr_parametro7;
		_xdr_result = (xdrproc_t) xdr_void;
		local = (bool_t (*) (char *, void *,  struct svc_req *))f_stat_1_svc;
		break;

	case F_QUIT:
		_xdr_argument = (xdrproc_t) xdr_void;
		_xdr_result = (xdrproc_t) xdr_int;
		local = (bool_t (*) (char *, void *,  struct svc_req *))f_quit_1_svc;
		break;

	default:
		svcerr_noproc (transp);
		return;
	}
	memset ((char *)&argument, 0, sizeof (argument));
	if (!svc_getargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		svcerr_decode (transp);
		return;
	}
	retval = (bool_t) (*local)((char *)&argument, (void *)&result, rqstp);
	if (retval > 0 && !svc_sendreply(transp, (xdrproc_t) _xdr_result, (char *)&result)) {
		svcerr_systemerr (transp);
	}
	if (!svc_freeargs (transp, (xdrproc_t) _xdr_argument, (caddr_t) &argument)) {
		fprintf (stderr, "%s", "unable to free arguments");
		exit (1);
	}
	if (!servicioprog_1_freeresult (transp, _xdr_result, (caddr_t) &result))
		fprintf (stderr, "%s", "unable to free results");

	return;
}

int
main (int argc, char **argv)
{
	register SVCXPRT *transp;

	pmap_unset (SERVICIOPROG, SERVICIOVERS);

	transp = svcudp_create(RPC_ANYSOCK);
	if (transp == NULL) {
		fprintf (stderr, "%s", "cannot create udp service.");
		exit(1);
	}
	if (!svc_register(transp, SERVICIOPROG, SERVICIOVERS, servicioprog_1, IPPROTO_UDP)) {
		fprintf (stderr, "%s", "unable to register (SERVICIOPROG, SERVICIOVERS, udp).");
		exit(1);
	}

	transp = svctcp_create(RPC_ANYSOCK, 0, 0);
	if (transp == NULL) {
		fprintf (stderr, "%s", "cannot create tcp service.");
		exit(1);
	}
	if (!svc_register(transp, SERVICIOPROG, SERVICIOVERS, servicioprog_1, IPPROTO_TCP)) {
		fprintf (stderr, "%s", "unable to register (SERVICIOPROG, SERVICIOVERS, tcp).");
		exit(1);
	}

	svc_run ();
	fprintf (stderr, "%s", "svc_run returned");
	exit (1);
	/* NOTREACHED */
}
